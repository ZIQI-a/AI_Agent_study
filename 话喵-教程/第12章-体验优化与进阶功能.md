# 第12章：体验优化与进阶功能

## 本章目标

**要做的事**：收藏功能、导出、多模型切换、暗色模式

**学到的知识**：
- UX 最佳实践
- 文件导出实现
- 主题切换
- 错误处理与重试

## 12.1 收藏功能

给文章和诗词添加收藏标记。

修改数据库 schema `src/lib/db/schema.ts`：

```typescript
// 在 articles 表中添加
export const articles = sqliteTable("articles", {
  // ... 已有字段
  isFavorite: integer("is_favorite", { mode: "boolean" }).default(false),
});

// 在 poems 表中添加
export const poems = sqliteTable("poems", {
  // ... 已有字段
  isFavorite: integer("is_favorite", { mode: "boolean" }).default(false),
});
```

执行迁移：

```bash
pnpm db:push
```

添加收藏 API：

```typescript
// src/app/api/articles/[id]/favorite/route.ts
import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { articles } from "@/lib/db/schema";
import { eq } from "drizzle-orm";

export async function PATCH(
  req: NextRequest,
  { params }: { params: { id: string } }
) {
  const { isFavorite } = await req.json();
  await db
    .update(articles)
    .set({ isFavorite })
    .where(eq(articles.id, Number(params.id)));
  return NextResponse.json({ success: true });
}
```

## 12.2 导出功能

### 导出为 Markdown 文件

创建 `src/lib/export/markdown.ts`：

```typescript
export function downloadMarkdown(content: string, filename: string) {
  const blob = new Blob([content], { type: "text/markdown;charset=utf-8" });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = `${filename}.md`;
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
  URL.revokeObjectURL(url);
}
```

在文章预览区添加导出按钮：

```typescript
// 在 CardHeader 的按钮区添加
<Button
  variant="outline"
  size="sm"
  onClick={() => downloadMarkdown(completion, title || "文章")}
>
  导出 Markdown
</Button>
```

### 导出为 PDF（使用浏览器打印）

```typescript
export function downloadPDF(elementId: string, filename: string) {
  const element = document.getElementById(elementId);
  if (!element) return;

  const printWindow = window.open("", "_blank");
  if (!printWindow) return;

  printWindow.document.write(`
    <html>
      <head>
        <title>${filename}</title>
        <style>
          body { font-family: sans-serif; max-width: 800px; margin: 0 auto; padding: 20px; }
          h1, h2, h3 { color: #333; }
          pre { background: #f5f5f5; padding: 12px; border-radius: 4px; }
          blockquote { border-left: 3px solid #ddd; padding-left: 12px; color: #666; }
        </style>
      </head>
      <body>${element.innerHTML}</body>
    </html>
  `);
  printWindow.document.close();
  printWindow.print();
}
```

## 12.3 多模型切换

创建模型配置 `src/lib/llm/models.ts`：

```typescript
import { createOpenAI } from "@ai-sdk/openai";

export interface ModelConfig {
  id: string;
  name: string;
  provider: string;
  baseURL: string;
  apiKey: string;
  model: string;
}

export const models: ModelConfig[] = [
  {
    id: "deepseek",
    name: "DeepSeek V3",
    provider: "DeepSeek",
    baseURL: "https://api.deepseek.com",
    apiKey: process.env.DEEPSEEK_API_KEY || "",
    model: "deepseek-chat",
  },
  {
    id: "qwen",
    name: "通义千问",
    provider: "阿里云",
    baseURL: "https://dashscope.aliyuncs.com/compatible-mode/v1",
    apiKey: process.env.QWEN_API_KEY || "",
    model: "qwen-turbo",
  },
];

export function getModel(modelId?: string) {
  const config = models.find((m) => m.id === modelId) || models[0];
  const client = createOpenAI({
    apiKey: config.apiKey,
    baseURL: config.baseURL,
  });
  return client(config.model);
}
```

在 API Route 中使用：

```typescript
// 替换之前的 deepseek("deepseek-chat")
import { getModel } from "@/lib/llm/models";

export async function POST(req: Request) {
  const { title, style, wordCount, detailLevel, modelId } = await req.json();

  const result = streamText({
    model: getModel(modelId), // 动态选择模型
    // ...
  });
}
```

前端添加模型选择器：

```typescript
// 在参数面板中添加
const [modelId, setModelId] = useState("deepseek");

<div className="space-y-2">
  <Label>AI 模型</Label>
  <Select value={modelId} onValueChange={setModelId}>
    <SelectTrigger>
      <SelectValue />
    </SelectTrigger>
    <SelectContent>
      <SelectItem value="deepseek">DeepSeek V3</SelectItem>
      <SelectItem value="qwen">通义千问</SelectItem>
    </SelectContent>
  </Select>
</div>
```

## 12.4 暗色模式

使用 `next-themes` 实现暗色模式：

```bash
pnpm add next-themes
```

修改 `src/app/layout.tsx`：

```typescript
"use client";

import { ThemeProvider } from "next-themes";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN" suppressHydrationWarning>
      <body>
        <ThemeProvider attribute="class" defaultTheme="light">
          {/* ... 布局内容 */}
        </ThemeProvider>
      </body>
    </html>
  );
}
```

在侧边栏底部添加主题切换按钮：

```typescript
"use client";

import { useTheme } from "next-themes";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <Button
      variant="ghost"
      size="sm"
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
    >
      {theme === "dark" ? "☀️" : "🌙"}
    </Button>
  );
}
```

## 12.5 错误处理与重试

创建通用错误处理组件 `src/components/ui/error-boundary.tsx`：

```typescript
"use client";

import { Button } from "@/components/ui/button";

interface ErrorDisplayProps {
  error: string;
  onRetry?: () => void;
}

export function ErrorDisplay({ error, onRetry }: ErrorDisplayProps) {
  return (
    <div className="text-center py-8">
      <div className="text-4xl mb-4">😿</div>
      <p className="text-muted-foreground mb-4">{error}</p>
      {onRetry && (
        <Button variant="outline" onClick={onRetry}>
          重试
        </Button>
      )}
    </div>
  );
}
```

在生成页面使用：

```typescript
{error && (
  <ErrorDisplay
    error={error.message}
    onRetry={handleGenerate}
  />
)}
```

## 12.6 加载状态优化

创建骨架屏组件 `src/components/ui/skeleton-card.tsx`：

```typescript
import { Skeleton } from "@/components/ui/skeleton";
import { Card, CardContent, CardHeader } from "@/components/ui/card";

export function SkeletonCard() {
  return (
    <Card>
      <CardHeader>
        <Skeleton className="h-6 w-48" />
      </CardHeader>
      <CardContent className="space-y-3">
        <Skeleton className="h-4 w-full" />
        <Skeleton className="h-4 w-5/6" />
        <Skeleton className="h-4 w-4/6" />
      </CardContent>
    </Card>
  );
}
```

## 12.7 字数统计改进

创建更精确的中文字数统计：

```typescript
// src/lib/utils/char-count.ts
export function countChineseChars(text: string): number {
  // 移除 Markdown 标记
  const plainText = text
    .replace(/#{1,6}\s/g, "")     // 标题
    .replace(/\*{1,3}/g, "")      // 加粗/斜体
    .replace(/`{1,3}[^`]*`{1,3}/g, "") // 代码
    .replace(/\[([^\]]*)\]\([^)]*\)/g, "$1") // 链接
    .replace(/\n/g, "");           // 换行

  // 统计中文字符
  const chineseChars = plainText.match(/[一-鿿]/g)?.length || 0;
  // 统计英文单词
  const englishWords = plainText.match(/[a-zA-Z]+/g)?.length || 0;

  return chineseChars + englishWords;
}
```

## 本章小结

| 功能 | 实现方式 |
|------|---------|
| 收藏 | 数据库字段 + API |
| 导出 Markdown | Blob + 下载链接 |
| 导出 PDF | 浏览器打印 |
| 多模型切换 | 配置化模型列表 |
| 暗色模式 | next-themes |
| 错误重试 | 错误展示组件 + 重试回调 |

## 下一章预告

功能都完成了！下一章我们把话喵部署到 Vercel，让它在公网上可以访问。
