# 学习路线图

## 总览：6 个月从前端到 AI 应用开发者

```
月 1          月 2          月 3          月 4          月 5          月 6
┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐
│ 基础 │───→│ 实战 │───→│ Agent│───→│  RAG │───→│ 进阶 │───→│ 项目 │
│ 概念 │    │ 对话 │    │ 入门 │    │ 知识 │    │ 架构 │    │ 实战 │
└──────┘    └──────┘    └──────┘    └──────┘    └──────┘    └──────┘
 LLM API      流式UI      Tools      向量检索     多Agent     生产级
 Prompt       对话管理     决策循环    知识管理     工作流      完整项目
 基本概念     结构化输出   状态管理    评估优化     LangGraph   部署上线
```

---

## 阶段 1：基础概念（第 1 月）

### 目标
理解 LLM 的工作原理，能独立调用 API 完成基本任务。

### 学习内容

**Week 1-2：LLM 基础与 API 调用**
- [ ] 理解 LLM 基本概念：Token、Temperature、Context Window
- [ ] 注册 OpenAI / DeepSeek API Key
- [ ] 用 TypeScript 调用 Chat Completion API
- [ ] 理解 System / User / Assistant 消息角色
- [ ] 练习基本的多轮对话

```typescript
// 第一周的练习目标：能跑通这个
import OpenAI from "openai";
const client = new OpenAI();

async function chat(userMessage: string) {
  const response = await client.chat.completions.create({
    model: "gpt-4o",
    messages: [
      { role: "system", content: "你是一个有帮助的助手" },
      { role: "user", content: userMessage },
    ],
  });
  return response.choices[0].message.content;
}
```

**Week 3-4：Prompt Engineering**
- [ ] 学习 Prompt 设计原则（清晰、具体、分步、示例）
- [ ] 掌握 Few-shot Prompting（给示例）
- [ ] 掌握 Chain-of-Thought（思维链）
- [ ] 学习结构化输出（JSON Schema）
- [ ] 练习：写一个能稳定输出 JSON 的 Prompt

```typescript
// 第四周练习目标：结构化输出
const response = await client.chat.completions.create({
  model: "gpt-4o",
  messages: [
    {
      role: "system",
      content: `分析用户的情感，严格按 JSON 格式输出：
{"sentiment": "positive"|"negative"|"neutral", "confidence": 0-1, "reason": "原因"}`
    },
    { role: "user", content: "这个产品太好用了！" }
  ],
  response_format: { type: "json_object" },
});
```

### 实践项目
**AI 翻译助手**：输入中文，输出英文，支持多种风格（正式/口语/技术）

### 学习资源
- OpenAI API 官方文档
- Prompt Engineering Guide（promptingguide.ai）
- DeepSeek API 文档（中文友好）

---

## 阶段 2：对话应用实战（第 2 月）

### 目标
能构建一个完整的流式对话应用，包含前端 UI 和后端 API。

### 学习内容

**Week 1-2：Vercel AI SDK 与流式响应**
- [ ] 理解 SSE（Server-Sent Events）原理
- [ ] 学习 Vercel AI SDK 的 `streamText` 和 `useChat`
- [ ] 构建 Next.js + AI SDK 的对话页面
- [ ] 实现打字机效果的流式渲染

```typescript
// 核心代码结构
// app/api/chat/route.ts
import { streamText } from "ai";
import { openai } from "@ai-sdk/openai";

export async function POST(req: Request) {
  const { messages } = await req.json();
  const result = streamText({
    model: openai("gpt-4o"),
    system: "你是一个有帮助的助手",
    messages,
  });
  return result.toDataStreamResponse();
}

// app/page.tsx
"use client";
import { useChat } from "@ai-sdk/react";

export default function Chat() {
  const { messages, input, handleInputChange, handleSubmit, isLoading } =
    useChat();
  return (
    <div>
      {messages.map((m) => (
        <div key={m.id}>
          <strong>{m.role}:</strong> {m.content}
        </div>
      ))}
      <form onSubmit={handleSubmit}>
        <input
          value={input}
          onChange={handleInputChange}
          placeholder="输入消息..."
        />
        <button type="submit" disabled={isLoading}>发送</button>
      </form>
    </div>
  );
}
```

**Week 3-4：对话管理与增强**
- [ ] 对话历史的存储与恢复
- [ ] System Prompt 的动态配置
- [ ] Markdown 渲染（react-markdown + 代码高亮）
- [ ] 错误处理与重试
- [ ] Token 用量统计

### 实践项目
**AI 助手 Web 应用**：
- 流式对话
- 对话历史侧边栏
- Markdown 渲染（支持代码块）
- 多模型切换
- 响应式设计

---

## 阶段 3：Agent 入门（第 3 月）

### 目标
理解 Agent 的核心机制，能构建一个带工具调用的简单 Agent。

### 学习内容

**Week 1-2：Function Calling**
- [ ] 深入理解 Function Calling 机制
- [ ] 学习工具定义（JSON Schema）
- [ ] 实现工具调用的完整流程
- [ ] 多工具并行调用

```typescript
// 工具定义
const tools = [
  {
    type: "function" as const,
    function: {
      name: "get_weather",
      description: "获取指定城市的当前天气",
      parameters: {
        type: "object",
        properties: {
          city: { type: "string", description: "城市名称，如'北京'" },
        },
        required: ["city"],
      },
    },
  },
];

// 调用
const response = await client.chat.completions.create({
  model: "gpt-4o",
  messages: [{ role: "user", content: "北京天气怎么样？" }],
  tools,
});

// 处理工具调用
if (response.choices[0].message.tool_calls) {
  const toolCall = response.choices[0].message.tool_calls[0];
  const result = await executeTool(toolCall.function.name,
    JSON.parse(toolCall.function.arguments));
  // 把结果发回 LLM...
}
```

**Week 3-4：构建简单 Agent**
- [ ] 理解 ReAct 模式
- [ ] 使用 LangGraph 构建 Agent
- [ ] 实现思考-行动-观察循环
- [ ] 工具执行结果的处理

```typescript
// LangGraph Agent
import { ChatOpenAI } from "@langchain/openai";
import { createReactAgent } from "@langchain/langgraph/prebuilt";

const agent = createReactAgent({
  llm: new ChatOpenAI({ model: "gpt-4o" }),
  tools: [weatherTool, calculatorTool, searchTool],
});

const result = await agent.invoke({
  messages: [{ role: "user", content: "北京和上海今天哪个更热？" }],
});
```

### 实践项目
**多功能助手 Agent**：
- 天气查询
- 计算器
- 网页搜索
- 文件读写
- 展示 Agent 的思考过程

---

## 阶段 4：RAG 知识库（第 4 月）

### 目标
能构建基于私有文档的知识问答系统。

### 学习内容

**Week 1-2：Embedding 与向量数据库**
- [ ] 理解 Embedding 原理（文本 → 向量）
- [ ] 学习向量相似度搜索
- [ ] 使用 Chroma / Pinecone 存储和检索
- [ ] 文档分块策略（Chunking）

**Week 3-4：构建 RAG 应用**
- [ ] 文档加载（PDF、Word、网页）
- [ ] 文档分块与向量化
- [ ] 检索与上下文拼接
- [ ] 回答生成与来源引用

```typescript
// RAG 核心流程
import { RecursiveCharacterTextSplitter } from "langchain/text_splitter";
import { OpenAIEmbeddings } from "@langchain/openai";
import { MemoryVectorStore } from "langchain/vectorstores/memory";

// 1. 文档分块
const splitter = new RecursiveCharacterTextSplitter({
  chunkSize: 1000,
  chunkOverlap: 200,
});
const docs = await splitter.splitDocuments(rawDocuments);

// 2. 向量化并存储
const vectorStore = await MemoryVectorStore.fromDocuments(
  docs,
  new OpenAIEmbeddings({ model: "text-embedding-3-small" })
);

// 3. 检索
const retriever = vectorStore.asRetriever({ k: 3 });
const relevantDocs = await retriever.invoke("年假政策是什么？");

// 4. 带上下文的问答
const context = relevantDocs.map(d => d.pageContent).join("\n\n");
const answer = await llm.invoke([
  { role: "system", content: `根据以下参考资料回答问题：\n${context}` },
  { role: "user", content: "年假政策是什么？" },
]);
```

### 实践项目
**企业知识库问答系统**：
- 支持上传 PDF/Word/Markdown
- 智能问答，附带来源引用
- 文档管理界面
- 检索结果的相关性排序

---

## 阶段 5：进阶架构（第 5 月）

### 目标
掌握复杂的 Agent 架构模式，能设计生产级应用。

### 学习内容

**Week 1-2：LangGraph 深入**
- [ ] 状态图（StateGraph）设计
- [ ] 条件分支与循环
- [ ] 人工确认节点（Human-in-the-loop）
- [ ] 持久化状态（Checkpoint）

**Week 3-4：Multi-Agent 与 Workflow**
- [ ] 多 Agent 协作模式
- [ ] Supervisor 架构
- [ ] 工作流设计与可视化
- [ ] Agent 评估与测试

```typescript
// Multi-Agent 示例
import { StateGraph, Annotation } from "@langchain/langgraph";

const StateAnnotation = Annotation.Root({
  messages: Annotation<any[]>({ reducer: (x, y) => [...x, ...y] }),
  nextAgent: Annotation<string>,
});

// 研究员 Agent
async function researcher(state) { /* 搜索和分析 */ }
// 编码员 Agent
async function coder(state) { /* 写代码 */ }
// 审核员 Agent
async function reviewer(state) { /* 审核结果 */ }

const graph = new StateGraph(StateAnnotation)
  .addNode("researcher", researcher)
  .addNode("coder", coder)
  .addNode("reviewer", reviewer)
  .addEdge("__start__", "researcher")
  .addConditionalEdges("researcher", (state) => state.nextAgent)
  // ...
```

### 实践项目
**代码审查 Agent**：
- 读取 PR 代码
- 分析代码质量
- 生成改进建议
- 多 Agent 协作（分析、建议、总结）

---

## 阶段 6：项目实战（第 6 月）

### 目标
完成一个可上线的 AI 应用，积累项目经验。

### 推荐项目方向（选一个深入）

**方向 A：AI 驱动的文档平台**
- 文档编辑 + AI 续写/改写
- 智能搜索（语义搜索）
- 自动摘要和标签

**方向 B：智能客服系统**
- 多轮对话
- 知识库问答
- 工单自动分类
- 人工接管

**方向 C：AI 编程助手**
- 代码补全
- 代码解释
- Bug 检测
- 重构建议

**方向 D：数据分析 Agent**
- 上传 CSV/Excel
- 自然语言查询
- 自动生成图表
- 报告生成

### 项目要求
- [ ] 完整的前端 UI
- [ ] 流式响应
- [ ] 错误处理和重试
- [ ] 用户认证
- [ ] 部署上线（Vercel / Railway）
- [ ] README 和演示

---

## 学习资源推荐

### 必读文档
1. **OpenAI API 文档** — api.openai.com/docs
2. **Vercel AI SDK 文档** — sdk.vercel.ai/docs
3. **LangChain 文档** — js.langchain.com
4. **LangGraph 文档** — langchain-ai.github.io/langgraph

### 推荐课程
1. **DeepLearning.AI - AI Agents in LangGraph**（Andrew Ng）
2. **DeepLearning.AI - Building Systems with the ChatGPT API**
3. **LlamaIndex - Building Performant RAG Applications**

### 推荐博客/社区
1. **LangChain Blog** — 最新 Agent 技术动态
2. **Vercel Blog** — 前端 AI 最佳实践
3. **Hugging Face** — 模型和数据集
4. **掘金 AI 专栏** — 中文社区

### 推荐开源项目（学习源码）
1. **Chatbot UI** — 开源 ChatGPT 前端
2. **Dify** — 开源 AI 应用平台
3. **Open WebUI** — 开源 LLM 前端
4. **Langchain-Chatchat** — 中文 RAG 项目

---

## 每日学习建议

```
工作日（1-2 小时）：
  - 30 分钟：阅读文档/教程
  - 30-60 分钟：动手写代码

周末（3-4 小时）：
  - 完成一个完整的小项目或功能模块
  - 整理笔记，写技术博客

学习原则：
  1. 概念理解后立刻动手写代码
  2. 每周至少跑通一个完整 demo
  3. 遇到问题先查文档，再问 AI
  4. 记录学习笔记，未来可以写博客
```
